[pytest]
# Tests import the app via package-relative imports (from ..routers import ...),
# so they must run with the repo root on sys.path and `src` as a package.
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = src/tests
filterwarnings =
    ignore::DeprecationWarning
